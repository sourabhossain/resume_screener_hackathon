# Capped below 6.0: the app image (python:3.11) resolves 5.2.x, but the
# Playwright verification image (python:3.12) would resolve Django 6.x, where
# `django.utils.cache.cc_delim_re` is gone and DRF fails to import — the
# verification worker crash-looped on startup. Keep both images on 5.2.
Django>=5.2,<6.0
# Production WSGI server (replaces the dev `runserver`) + static file serving.
gunicorn>=21.2
whitenoise>=6.6
# Must match the Playwright version of the verification base image in the
# Dockerfile — that image supplies the browser binaries and we deliberately
# skip `playwright install`. A newer pip package looks for a browser build the
# image doesn't have and every crawl dies with "Executable doesn't exist".
playwright==1.60.0
httpx>=0.26.0
httpcore>=1.0.0
PyMySQL>=1.1
cryptography>=42.0
python-dotenv>=1.0
Pillow>=10.0
requests

# Testing
pytest>=8.0
pytest-django>=4.7
factory-boy>=3.3
pytest-cov>=4.1

# API
djangorestframework>=3.14
drf-spectacular>=0.27

# Security
django-ratelimit>=4.1

# Markdown rendering
markdown>=3.5

# Streaming ZIP downloads (memory-safe bulk CV export)
zipstream-ng>=1.7.0

# AI/NLP
langgraph>=0.2
langchain>=0.3
langchain-openai>=0.2
openai>=1.0
pypdf>=4.0.0
pymupdf>=1.24
python-docx>=1.0
pydantic>=2.0
tenacity>=8.0

# Async Tasks
celery>=5.3
redis>=5.0

# Monitoring & Logging
sentry-sdk>=1.0
python-json-logger>=2.0